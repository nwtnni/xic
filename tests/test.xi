use io
use conv

main(args:int[][]) {
    // x:int = 10

    // a:int[x]

    // x = x - 1
    // while (x >= 0) {
    //     a[x] = x
    //     print(unparseInt(a[x]))
    //     x = x - 1
    // }

    x:int[] = "H" + "ii"
    println(x)

    // print(unparseInt(x) + " + " + unparseInt(y) + " = " + unparseInt(x+y))
}


printInt(x:int) {
    println(unparseInt(x))
}
