package optimize;

import ir.*;
import util.Graph;

public class IRGraph extends Graph<IRNode, Object> {

    // Construct from IRFuncDecl()

    // We will need a way to number nodes to use as keys 
    // Perhaps a wrapper class?
    // Add metadata value to each node

    // toIRFuncDecl()
	
}
